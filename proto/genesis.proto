syntax = "proto3";

message ReplyBase {
    int32 status = 1;
    string message = 2;
}

message RunScalerRequest {
    string scalerName = 1;
}

message RunScalerReply {
    ReplyBase status = 1;
}

service GenesisRM {
    rpc RunScaler(RunScalerRequest) returns (RunScalerReply) {}
}